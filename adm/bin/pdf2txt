#!/bin/sh


pdftotext $1 |
    sed 's/^[ 	]*//' |
    awk '{if ($0 ~ /-$/) {printf("%s", substr($0,0,length($0)-1))} else {print $0}}' |
    awk '{if (length($0) == 0) {print $0} else {printf("%s ", $0)}}' 
    > `basename $1 pdf`.txt
