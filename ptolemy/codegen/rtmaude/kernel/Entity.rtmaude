/***moduleName***/
ACTOR/**/

/***checkModuleName***/
CHECK-ACTOR/**/

/***className***/
Actor/**/

/***termBlock***/
#model('$actorSymbol(model))/**/

/***fireBlock($attr_terms)***/
< '$info(name) : $block(className) |
$attr_terms
>/**/

/***attr_Actor***/
store : emptyMap,
status : $block(statusBlock),
ports : ($indent(
  $info(ports) 
)),
parameters : ($indent( 
  $info(parameters)
))/**/

/***statusBlock***/
enabled/**/
