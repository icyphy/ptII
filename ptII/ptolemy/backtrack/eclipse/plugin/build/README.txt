The directory that contains the build file(s) for the plugin.

To build the plugin, first compile the whole project in Eclipse, then run
build.xml with Eclipse built-in Ant builder. The packages will be placed in
this directory.
