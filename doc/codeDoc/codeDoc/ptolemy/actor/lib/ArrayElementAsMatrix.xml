<?xml version="1.0" standalone="yes"?>
<!DOCTYPE doc PUBLIC "-//UC Berkeley//DTD DocML 1//EN"
    "http://ptolemy.eecs.berkeley.edu/xml/dtd/DocML_1.dtd">
<doc name="ArrayElementAsMatrix" class="ptolemy.actor.lib.ArrayElementAsMatrix">
  <description>
Extract an element from an array by accessing the array as a matrix.&#10;&#10; This actor reads an array from the&#10; &lt;i&gt;input&lt;/i&gt; port and sends one of its elements to the &lt;i&gt;output&lt;/i&gt;&#10; port.  The element that is extracted is determined by the&#10; &lt;i&gt;x&lt;/i&gt;, &lt;i&gt;y&lt;/i&gt;, &lt;i&gt;xOffset&lt;/i&gt; and &lt;i&gt;yOffset&lt;/i&gt;  parameters (or port).&#10; It is required that&#10; &lt;pre&gt;&#10; 0 &amp;lt;= &lt;i&gt;x&lt;/i&gt; * &lt;i&gt;column&lt;/i&gt; + &lt;i&gt;y&lt;/i&gt; * &lt;i&gt;yOffset&lt;/i&gt;  &amp;lt; &lt;i&gt;N&lt;/i&gt;&#10; &lt;/pre&gt;&#10; where &lt;i&gt;N&lt;/i&gt; is the length of the input array, or an exception will&#10; be thrown by the fire() method.  </description>
  <author>Christopher Brooks</author>
  <version>$Id$</version>
  <since>Ptolemy II 6.1</since>
  <Pt.ProposedRating>Red (cxh)</Pt.ProposedRating>
  <Pt.AcceptedRating>Red (cxh)</Pt.AcceptedRating>
    <!--ptolemy.actor.parameters.PortParameter-->
    <property name="xOffset">The xOffset offset into the input array.  This is an integer that&#10;  defaults to 1. If the port is left unconnected, then the&#10;  parameter value will be used.</property>
    <!--ptolemy.actor.parameters.PortParameter-->
    <property name="yOffset">The yOffset offset into the input array.  This is an integer that&#10;  defaults to 1. If the port is left unconnected, then the&#10;  parameter value will be used.</property>
    <!--ptolemy.actor.parameters.PortParameter-->
    <property name="x">The x index into the input array.  This is an integer that&#10;  defaults to 0. If the port is left unconnected, then the&#10;  parameter value will be used.</property>
    <!--ptolemy.actor.parameters.PortParameter-->
    <property name="y">The y index into the input array.  This is an integer that&#10;  defaults to 0. If the port is left unconnected, then the&#10;  parameter value will be used.</property>
</doc>
