import ptolemy.actor.ptalon.test.Combine;

Parallel is {
	parameter repeater;
	intparameter n;
	if (n <= 1) {
		repeater();
	} else {
		repeater();
		Parallel(repeater := repeater(), n := n - 1);
	}
}
