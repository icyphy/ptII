/*** preinitBlock ***/
int $actorSymbol(i);
/**/

/*** fireBlock ***/
for ($actorSymbol(i) = 0; $actorSymbol(i) < $val(arrayLength); $actorSymbol(i)++) {
    Array_set($ref(output),$actorSymbol(i), $ref((Token)input));
}	
/**/
